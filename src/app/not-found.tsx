import { NotFound as NotFoundComponent} from '../components/ui';

export default function NotFound() {
    return (
        <NotFoundComponent message='Page not found, go back to the beginning to see interesting topics.' />
    )
}